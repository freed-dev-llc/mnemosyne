# Labelled retrieval question set for the Ubiquiti / UniFi pack.
#
# Ground truth = expected substrings (see ADR-0006), each drawn verbatim from a local source
# file in sources/ (the seed note plus the unifi-* primers). A question "hits" iff EVERY
# `expected` item is satisfied (case-insensitive) in the concatenated top-k retrieved chunks.
# An item is either a single required substring, or a list of interchangeable alternatives (an
# OR-group) of which any one satisfies it. Use an OR-group when the same fact is worded
# differently across sources, so a correct answer counts regardless of which source supplies it
# (e.g. a fetched doc's "Native (untagged) VLAN" for the seed's "untagged network"). Author
# questions only from text that is actually in the corpus; if a fact isn't there, it doesn't
# belong here. The first block covers the seed (core model); the second covers the security,
# RF, and operations primers (added in ADR-0014, issue #41).
#
# A question may carry `corpus: fetched` (ADR-0020): its ground truth lives in the fetched
# Help Center pages, so it is only answerable on a served-style (URL-inclusive) index. The
# loader EXCLUDES those by default; the CI-gated, local-only eval population is the untagged
# questions above, and `mnemosyne eval --include-fetched` opts a served run into the rest.
# Sourcing policy for fetched ground truth (identifier-anchored, ADR-0020): expected strings
# are restricted to non-creative technical material: port numbers, protocol/standard names,
# commands, product/feature/UI names, acronyms, and where truly needed a factual phrase of a
# few words. No third-party prose. The third block below follows that policy.
questions:
  - id: unifi-model
    question: How is a UniFi network organized around the controller?
    expected:
      - controller
      - "switches, access points, gateways"
      - site
    note: The controller/devices/site split.

  - id: controller-vs-device
    question: In the UniFi model, who decides configuration and who applies it?
    expected:
      - "the controller decides, the device enforces"

  - id: adoption-lifecycle
    question: What stages does a device move through during adoption?
    expected:
      - Pending Adoption
      - Adopting
      - Provisioning
      - Connected

  - id: adoption-prerequisites
    question: What does a device need in order to be adopted by a controller?
    expected:
      - Layer 3
      - reachability
      - inform URL

  - id: adoption-loop
    question: What is an adoption loop and what commonly causes it?
    expected:
      - adoption loop
      - factory reset

  - id: set-inform
    question: How do you point a UniFi device at its controller manually over SSH?
    expected:
      - "set-inform http://<host>:8080/inform"

  - id: port-profiles
    question: What does a UniFi port profile bundle together?
    expected:
      - port profile
      - native
      - tagged networks

  - id: trunk-vs-access
    question: What distinguishes a trunk port from an access port on a UniFi switch?
    expected:
      - - untagged network
        - native (untagged)
      - tagged VLANs
      - trunk

  - id: poe-cycle
    question: How can you power-cycle a stuck access point from a UniFi switch?
    expected:
      - PoE
      - power-cycling a stuck AP

  - id: wlan-vlan
    question: How is guest vs IoT vs main separation expressed in UniFi wireless?
    expected:
      - WLAN
      - VLAN-backed networks

  # --- Questions over the unifi-* primers (security, RF, operations; ADR-0014, issue #41) ---

  - id: vlan-segmentation
    question: Why segment a UniFi network into VLANs?
    expected:
      - blast radius
      - trust levels apart
    note: From the network-security primer.

  - id: firewall-default-deny
    question: What firewall posture should a UniFi gateway start from?
    expected:
      - default-deny
      - block traffic between segments

  - id: wpa3-pmf
    question: What does WPA3 add over WPA2, and what is PMF?
    expected:
      - Simultaneous Authentication of Equals
      - forward secrecy
      - Protected Management Frames

  - id: channel-2-4ghz
    question: Which 2.4 GHz channels and channel width should a UniFi network use?
    expected:
      - "non-overlapping channels: 1, 6, and 11"
      - 20 MHz

  - id: roaming-amendments
    question: Which 802.11 amendments improve roaming between UniFi access points?
    expected:
      - 802.11r
      - 802.11k
      - 802.11v

  - id: minimum-rssi
    question: What does the minimum RSSI setting do, and what values are aggressive vs conservative?
    expected:
      - Minimum RSSI
      - "-75 dBm is aggressive"
      - "-85 dBm conservative"

  - id: stuck-adopting
    question: How do you recover a UniFi device stuck in the Adopting state?
    expected:
      - Forget the device
      - port 8080
      - set-inform

  - id: udm-se-firmware
    question: Why must UDM SE and UDM Pro firmware be upgraded over SSH instead of the controller API?
    expected:
      - UDM SE
      - cannot upgrade them
      - ubnt-systool fwupdate

  - id: firmware-backup
    question: What should you do before upgrading UniFi firmware?
    expected:
      - controller backup
      - read the release notes

  # Fetched-coverage questions (ADR-0020): ground truth drawn from the fetched Help
  # Center pages under the identifier-anchored policy; only in the index when the URL
  # corpus is ingested, so each carries `corpus: fetched` and is excluded from the
  # default (local-only, CI-gated) eval population.
  - id: dhcp-option-43
    question: How can a DHCP server tell a UniFi device where the remote Network Application lives?
    expected:
      - option 43
    corpus: fetched
    note: Remote Adoption (Layer 3); DHCP-based inform.

  - id: l3-adoption-port
    question: What connectivity must exist between a UniFi device and a remote Network Application for Layer 3 adoption?
    expected:
      - TCP Port 8080
    corpus: fetched
    note: Remote Adoption (Layer 3) requirements.

  - id: setup-blocked-ports
    question: Which ports do ISP modems or third-party firewalls sometimes block that break UniFi setup?
    expected:
      - "8883"
      - "3478"
    corpus: fetched
    note: How to Set Up UniFi, required-ports call-out.

  - id: stun-port
    question: Which protocol and port does UniFi use for STUN?
    expected:
      - STUN
      - "3478"
    corpus: fetched
    note: Required Ports Reference.

  - id: mdns-forwarding
    question: How can Chromecast or AirPlay devices be discovered across VLANs in UniFi?
    expected:
      - mDNS
      - Chromecast
    corpus: fetched
    note: UniFi Switch Settings, mDNS forwarding.

  - id: igmp-snooping
    question: Which UniFi switch feature reduces multicast flooding?
    expected:
      - IGMP Snooping
      - multicast
    corpus: fetched
    note: UniFi Switch Settings.

  - id: acl-unsupported-models
    question: Which UniFi switch models do not support ACLs?
    expected:
      - FlexMini
      - USW Industrial
    corpus: fetched
    note: UniFi Switches and ACLs, requirements.

  - id: mac-acl-same-vlan
    question: Why use switch ACLs instead of firewall rules for isolating devices on the same VLAN?
    expected:
      - MAC ACL
      - firewall rules
    corpus: fetched
    note: Switch ACLs vs firewall rules.

  - id: radius-dynamic-vlan
    question: How can UniFi assign a VLAN based on a user's login credentials?
    expected:
      - RADIUS
      - 802.1X
    corpus: fetched
    note: Creating Virtual Networks, dynamic VLAN assignment.

  - id: vlan-magic
    question: What UniFi shortcut creates VLANs straight from the topology view on smaller sites?
    expected:
      - VLAN Magic
      - Topology
    corpus: fetched
    note: Creating Virtual Networks.

  - id: zone-matrix
    question: Where does UniFi visually show the policies between firewall zones?
    expected:
      - Zone Matrix
    corpus: fetched
    note: Zone-Based Firewalls.

  - id: l3-switch-routing
    question: What happens when a Virtual Network's Router option is set to a UniFi Switch?
    expected:
      - Router
      - Inter-VLAN routing
    corpus: fetched
    note: Layer 3 Routing page.

  - id: guest-client-isolation
    question: How do you stop guest WiFi clients on the same access point from reaching each other?
    expected:
      - Client Device Isolation
      - ACL
    corpus: fetched
    note: Best Practices Guest WiFi.
